# helm-chart-experiment
This project is an experimental Helm chart for deploying nginx with configurable settings. 
The chart allows users to set three values: replicaCount (from 1 to 10), webPageContent, and vaultEnabled (to enable or disable Hashicorp Vault as a sidecar container).
